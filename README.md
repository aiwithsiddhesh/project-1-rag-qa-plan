# Document Q&A RAG System

This repository is being built phase by phase from `project-1-rag-qa-plan.md`.

Current status: Phase 1 foundation modules.

Implemented:

- Pydantic settings in `src/config.py`
- RAG exception hierarchy in `src/exceptions.py`
- Logging and utility helpers in `src/utils.py`

Planned commands:

- `make setup`
- `make ingest`
- `make serve`
- `make ui`
- `make test`
- `make eval`
